%h1 Editing task
= form_for @task, url: admin_user_task_path(current_user, @task), method: :put do |form|
  = render 'form', f: form

/ = link_to 'Show', @task
/ \|
= link_to 'Back', admin_user_tasks_path(current_user)
